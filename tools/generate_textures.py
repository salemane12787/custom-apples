"""DISABLED — use AI textures only. Run: python tools/install_ai_texture.py"""
import sys

sys.exit(
    "Refusing to run procedural texture generator. "
    "Use AI images via: python tools/install_ai_texture.py"
)
