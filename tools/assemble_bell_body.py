"""DISABLED — use AI textures only. Run: python tools/install_ai_texture.py --placed-blocks"""
import sys

sys.exit(
    "Refusing to run procedural bell body. "
    "Use AI images via: python tools/install_ai_texture.py --placed-blocks"
)
